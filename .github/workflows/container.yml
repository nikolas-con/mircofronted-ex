name: deploy-container

on:
  push:
    branches:
      - main
    paths:
      - "packages/**"

defaults:
  run:
    working-directory: packages/container

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - run: npm install
      - run: npm run build

      - uses: chrislennon/action-aws-cli@v1.1
      - run: aws s3 sync dist s3://microfronts-ex/container/latest
        env:
          AWS_ACCESS_KEY_ID: AKIATOEI5JQPZV4X6XB7
          AWS_SECRET_ACCESS_KEY: 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
